import styled from "styled-components";

export const DirectionsIframe = styled.iframe`
  border: 0;
  display: block;
  margin: 0 auto;
  height: 50vh;
  width: 100%;
`;

export const DirectionsList = styled.ol`
  margin-left: 1rem;
  padding-left: 1rem;
`;
