import styled from "styled-components";
import { COLORS, ThanksgivingPageWrapper } from "../../styles";

export const HomepageWrapper = styled(ThanksgivingPageWrapper)`
  padding: 0.5rem 0;
  max-width: 100%;

  a {
    color: ${COLORS.sand};
  }
`;
